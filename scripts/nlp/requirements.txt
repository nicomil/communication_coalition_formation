# Dipendenze della pipeline di analisi del testo.
#
#   pip install -r scripts/nlp/requirements.txt
#
# Lo stadio delle misure testuali deterministiche (text_metrics, aggregate)
# gira con la sola libreria standard: nulla qui sotto e' necessario per
# eseguirlo. Le voci servono agli stadi opzionali.

# Sentiment validato e open source. Senza, text_metrics ripiega sui dizionari
# di semi e lo segnala nella colonna sentiment_backend.
vaderSentiment>=3.3.2

# Rubrica di validazione (scripts/nlp/llm_rubric.py) e client di TopicGPT.
# Il fornitore della rubrica e' intercambiabile: basta una delle due librerie,
# quella corrispondente alla chiave che si usa. `openai` serve comunque a
# TopicGPT, ed e' anche il client dei modelli locali via Ollama.
openai>=1.54
pydantic>=2.0
anthropic>=0.40   # solo se si sceglie Claude come giudice della rubrica

# TopicGPT (Pham et al., 2024) non si installa da qui: i file di prompt fanno
# parte del metodo e stanno nel repository, non nel pacchetto pubblicato.
#   git clone https://github.com/chtmp223/topicGPT.git
#   pip install ./topicGPT
# Installare dal repository e non da PyPI: nella release 0.2.7 vLLM e' importato
# al primo livello e non e' installabile su macOS senza GPU, mentre sul ramo
# main e' gia' diventato un extra opzionale.
